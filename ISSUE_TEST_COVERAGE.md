## Prioritized List of Untested Modules/Functions

Based on analysis of: (1) number of dependents, (2) frequency of recent changes, (3) complexity.

### HIGH PRIORITY (Critical - high traffic/dependents)

#### 1. Backend: `handlers/public_booking_handler.go`
- **Why:** Public-facing API endpoint (`POST /api/public/bookings`) - highest user interaction
- **Complexity:** 104 lines, validation, error mapping, booking creation flow
- **Missing:** Unit tests for `Create()` method - handles guest booking creation with conflict detection
- **Dependents:** Guests booking flow, event type availability checks

#### 2. Backend: `handlers/public_event_type_handler.go`
- **Why:** Public API (`GET /api/public/event-types`, `/api/public/slots`) - guest discovery
- **Complexity:** 104 lines, dual service dependencies (EventType + TimeSlot services)
- **Missing:** Unit tests for all 3 methods: `List()`, `GetByID()`, `GetSlots()`
- **Dependents:** Guest home page, booking page slot availability

#### 3. Backend: `handlers/helpers.go`
- **Why:** Core utility used by ALL handlers - every HTTP response passes through here
- **Complexity:** 90 lines, 10 exported functions for response formatting
- **Missing:** Tests for `SuccessResponse`, `ErrorResponse`, `ValidationError`, `NotFound`, `BadRequest`, `InvalidTime`, `Conflict`, `NoContent`, `lowerFirst`, `validationMessage`
- **Dependents:** All 7 handler files

### MEDIUM PRIORITY (API endpoints with moderate traffic)

#### 4. Backend: `handlers/booking_handler.go`
- **Why:** Owner API for booking management (`GET/DELETE /api/bookings`)
- **Complexity:** 97 lines, 3 endpoints
- **Missing:** Tests for `List()` (with date filtering), `Cancel()` (with already-canceled edge case)
- **Dependents:** Owner dashboard bookings list

#### 5. Backend: `handlers/time_slot_handler.go`
- **Why:** Owner slot management (`GET /api/slots`, `POST /slots/generate`)
- **Complexity:** 144 lines, complex query parameter parsing, slot generation validation
- **Missing:** Tests for `List()` (multiple filter params), `GenerateSlots()` (validation + generation)
- **Dependents:** SlotGeneration component, owner availability management

#### 6. Backend: `handlers/event_type_handler.go`
- **Why:** CRUD for event types (`POST/GET/PATCH/DELETE /api/event-types`)
- **Complexity:** 120 lines, 5 endpoints, owner context handling
- **Missing:** Tests for `Create()`, `Update()`, `Delete()` - especially owner context extraction
- **Dependents:** EventTypeManagement component

#### 7. Backend: `handlers/owner_handler.go`
- **Why:** Owner resource management (`POST/GET /api/owners`)
- **Complexity:** 56 lines, 2 endpoints
- **Missing:** Tests for `Create()`, `GetByID()`
- **Dependents:** Owner onboarding flow

### LOW PRIORITY (Internal/infrastructure)

#### 8. Frontend: `api/client.ts`
- **Why:** API client with error handling
- **Complexity:** ~150 lines, fetch wrapper with error transformation
- **Missing:** More comprehensive error path tests, retry logic tests
- **Note:** Partial coverage exists in `client.test.ts`

#### 9. Frontend: `components/owner/BookingsList.tsx`
- **Why:** Owner bookings display component
- **Missing:** More edge case tests (empty state, loading states)

#### 10. Frontend: `pages/owner/OwnerDashboard.tsx`
- **Why:** Main owner dashboard
- **Missing:** Additional integration tests

---

## Test Strategy Recommendations

1. **Start with helpers.go** - tests here will improve all other handler tests
2. **Then public handlers** - they have highest external impact (guests)
3. **Then owner handlers** - internal management, lower risk if untested
4. **Frontend** - focus on api/client.ts error handling first

Current coverage: ~18% (per CI). Goal: 25%+ with these additions.

---

*Generated by codebase analysis - see AGENTS.md for methodology*